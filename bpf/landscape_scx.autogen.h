/*
 * Default generated scheduler header.
 *
 * The real file is emitted into the custom_bpf build directory next to a
 * temporary source copy. Keep this checked-in copy valid so the source remains
 * buildable even before the runtime generator runs.
 */

#ifndef LANDSCAPE_GEN_SCX_FLAGS
#define LANDSCAPE_GEN_SCX_FLAGS SCX_OPS_SWITCH_PARTIAL
#endif
